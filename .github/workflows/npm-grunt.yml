name: Project Delivery CI/CI

on:
  push:
    branches: [ "main" ]

jobs:
  Test:
    name: 🐛 Unit Tests
    runs-on: ubuntu-latest
    steps:
      - run: npm install --save-dev jest
      - run: jest --
